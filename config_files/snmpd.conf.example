#
# Arquivo de configuração para o daemon Net-SNMP (snmpd)
#
# Para mais informações, consulte as páginas de manual snmpd.conf(5) e snmpd(8).
#

###############################################################################
# ACESSO
###############################################################################

# Defina sua string de comunidade para acesso somente leitura.
# Substitua 'your_community_string' por uma string forte e única.
# Por exemplo: rocommunity public
rocommunity your_community_string

# Para permitir acesso de IPs específicos (opcional, mas recomendado para segurança)
# Agente escuta em todas as interfaces IPv4 e IPv6 na porta 161
agentAddress udp:161,udp6:[::]:161

# Exemplo para permitir acesso da comunidade 'public' de qualquer host (menos seguro)
# com2sec notConfigUser  default       public

# Exemplo para permitir acesso da comunidade 'public' de uma rede específica
# com2sec mynetwork      192.168.1.0/24 public


###############################################################################
# INFORMAÇÕES DO SISTEMA
###############################################################################

# Localização do sistema
syslocation Servidor Local PUC Goias Redes 2

# Contato do sistema
syscontact aluno@pucgoias.edu.br

# Serviços do sistema (valores comuns: 72 para serviços de rede, 6 para aplicações)
sysservices 72


###############################################################################
# VISUALIZAÇÕES DE MIB
###############################################################################

# Defina quais partes da árvore MIB são visíveis para cada grupo.
# 'systemview' é uma visualização comum que inclui informações básicas do sistema.
# Garanta que estes OIDs estejam incluídos para as informações desejadas.

# Visualização padrão para SNMPv2-MIB
view   systemview included .1.3.6.1.2.1.1   # SNMPv2-MIB::system
view   systemview included .1.3.6.1.2.1.2   # IF-MIB::interfaces (para ifNumber)
view   systemview included .1.3.6.1.2.1.25.1.1 # hrSystem
view   systemview included .1.3.6.1.2.1.25.2.1 # hrStorage
view   systemview included .1.3.6.1.2.1.25.3.1 # hrDevice
view   systemview included .1.3.6.1.2.1.25.4.1 # hrSWRun
view   systemview included .1.3.6.1.2.1.25.5.1 # hrSWRunPerf
view   systemview included .1.3.6.1.2.1.25.6.1 # hrSWInstalled

# Para CPU idle, memória total e memória livre (OIDs UCD-SNMP-MIB)
# Estes são OIDs específicos da MIB UCD-SNMP, que é comum em sistemas Linux
# rodando Net-SNMP. Eles geralmente funcionam se o agente estiver configurado.
# Certifique-se de que o módulo 'ucd-snmp/lm' ou similar esteja carregado,
# o que geralmente é feito por padrão ou por diretivas como 'disk' ou 'load'.

# Permite acesso à MIB UCD-SNMP.
# Se você tiver problemas para ler CPU/Memória, certifique-se de que essas linhas
# não estejam comentadas ou que o agente esteja compilado com suporte UCD-SNMP.
# disk   /
# load   12 10 5
# proc   sshd


###############################################################################
# MISC
###############################################################################

# Descomente para logar no syslog
# dontLogTCPWrappers off

# Define o nome do host para relatórios SNMP
# agentCotnact admin@example.com

# Define o nome do processo
# process snmpd